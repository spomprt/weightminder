server:
  port: ${PORT}

telegram:
  bot:
    token: 6870085716:AAH0SbM-KZSR4yb-rozhb4-2rlsI9Kb451s

spring:
  datasource:
    url: jdbc:postgresql://amvera-spomprt-cnpg-db-weightminder-rw:5432/weightminder
    username: spomprt
    password: spomprt
  cloud:
    openfeign:
      client:
        config:
          shortener-client:
            default-request-headers:
              x-rapidapi-key: a9388f8939msh947f0fe994d4633p14e2e6jsnb35bb2c22af2

cache:
  redis:
    host: ${CACHE_REDIS_HOST}
    port: ${CACHE_REDIS_PORT}
    user: ${CACHE_REDIS_USER}
    password: ${CACHE_REDIS_PASSWORD}

external:
  shortener:
    url: https://blinkify.up.railway.app/blinkify/api/v1
  quickchart:
    url: https://quickchart.io/chart?c=

javamelody:
  enabled: true

